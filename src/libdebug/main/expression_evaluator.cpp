#include "expression_evaluator.h"

bool ExpressionEvaluator::evaluate(const std::string& expression, ICore* cpu, IBus* bus) {
    if (expression.empty()) {
        return true;
    }
    // TODO: Implement expression parsing and evaluation
    return true;
}
